server:
  port: 3001
spring:
  application:
    name: flight-management-service
  sql:
    init:
      mode: always
  r2dbc:
    url: r2dbc:mysql://localhost:3306/flight_alert?useServerPrepStmts=false&sslMode=DISABLED
    username: root
    password: Andres20
    pool:
      enabled: true
      initial-size: 5
      max-size: 20
      max-idle-time: 30m
      validation-query: SELECT 1